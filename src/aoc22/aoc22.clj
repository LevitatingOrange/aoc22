(ns aoc22.aoc22
  "FIXME: my new org.corfield.new/scratch project.")

(defn exec
  "Invoke me with clojure -X aoc22.aoc22/exec"
  [opts]
  (println "exec with" opts))

(defn -main
  "Invoke me with clojure -M -m aoc22.aoc22"
  [& args]
  (println "-main with" args))
